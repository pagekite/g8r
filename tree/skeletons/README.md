# Server Skeletons

These are "host skeletons",
each directory is a skeleton of a particular server type,
telling the Governator which recipes to apply
(symbolic links to `../recipes`)
and optionally which configuration files or data to include by default.

Skeletons are used by `g8r add-host`.
